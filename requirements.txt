# Web scraping
requests==2.31.0
beautifulsoup4==4.12.3
feedparser==6.0.11
lxml==5.3.0
selenium>=4.20.0

# Data validation
pydantic==2.10.6

# Database
supabase>=2.9.0

# Redis
upstash-redis==1.6.0

# AI/Translation
# Gemini는 REST(generativelanguage.googleapis.com)를 requests 로 직접 호출한다.
# 구 SDK(google-generativeai)는 thinking 설정을 지원하지 않고 버전 호환 이슈가 있어 제거함.

# Utilities
python-dotenv==1.0.1